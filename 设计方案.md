# 日本行程PWA小工具 · 设计方案

状态:已和用户逐节确认,待写入实现计划
数据源:D:\Projects\日本旅游\行程确定版-0912-0920.md(当前v25)

## 背景与目标

Carey 2026年9月12–20日单人日本行,现有行程记录在markdown文档+html图册里,是行前电脑规划用的。这次要做一个**旅行途中在手机上用**的小工具,取代现有文档/图册在旅途中的使用场景(行前文档仍然保留作为规划底稿)。

## 需求(已确认)

- **使用场景**:日本当地手机查看,不是行前规划工具
- **离线**:地铁/户隐山区可能没信号,需要离线兜底
- **形态**:PWA——浏览器打开后"添加到主屏幕",全屏无地址栏,不用上架App Store/开发者账号
- **交互功能**:
  1. 打开自动定位到"今天"对应的D#
  2. 预订清单可点击打勾,状态存本地(localStorage),关闭重开不丢失
  3. 实时倒计时下一个行程项还有多久
  4. 地图交互(缩放拖动、点红点弹信息),离线也能看
- **数据归属**:做好后取代现有md文档/html图册在旅途中的使用,以后行程变动只改这个App背后的数据文件

## 技术方案

**方案A(已选定)**:纯手写PWA,无构建框架——HTML+CSS+原生JS+Leaflet.js画交互地图+手写Service Worker管离线缓存。理由:9天个人行程用不上框架的工程化收益,vanilla写法部署简单(GitHub Pages直接托管静态文件),依赖只有Leaflet一个,便于长期维护。

其他考虑过的方案:
- 方案B(未选):Vite+Svelte/Preact框架+PWA插件,更工程化但要装Node构建环境,对个人9天行程偏重,已否决
- 方案C(已否决):Notion等无代码模板,做不到交互地图+离线缓存+勾选+倒计时这几个核心需求

## 架构

```
pwa/
├── index.html          # 单入口,顶部D1-D9切换条
├── manifest.json        # PWA元数据(图标/名称/主题色)
├── sw.js                # Service Worker,管理离线缓存
├── data/
│   ├── d1.json ~ d9.json   # 每天一份行程数据
│   └── checklist.json      # 预订清单条目
└── js/
    ├── app.js            # 渲染逻辑、今日定位、倒计时
    ├── map.js             # Leaflet地图交互、瓦片预热缓存
    └── checklist.js       # 预订清单打勾状态,存localStorage
```

数据和展示分离——行程变动(比如某家餐厅从"候补中"变成"已确认")只改对应的`d#.json`,不用碰`app.js`。

## 数据模型

每天一份`data/d#.json`:

```json
{
  "day": 3,
  "date": "2026-09-14",
  "title": "户隐神社奥社一日往返(长野)",
  "items": [
    {
      "time": "19:00",
      "endTime": "21:00",
      "title": "晚饭②:寿司",
      "detail": "Harutaka(青空)候补中",
      "status": "pending",
      "lat": 35.671,
      "lng": 139.765
    }
  ]
}
```

- `status`取值:`confirmed`(✅)/`pending`(⚠)/不写则为普通项。直接对应现有文档里已经在用的✅/⚠标记体系
- 待定/多候选的行程项(比如"D3晚饭鮨みずかみ+Harutaka二选一"这种):**结果出来之前不写进数据里**,只用`pending`状态+一句话说明当前在等什么;等确定了再改成`confirmed`并补全具体信息(选定的方案B)

`data/checklist.json`:单独存放当前还没落地的事项列表(纯文本,比如"D3晚饭还没定""新宿酒店要不要升级成WPU"),每条一个唯一id。打勾状态存在localStorage的独立对象里(不和数据文件混在一起),这样我更新`checklist.json`内容时不会冲掉用户已经点掉的勾选状态。

## 核心组件逻辑

**今日定位**(app.js):
- 拿手机当前时间比对9/12–9/20这个范围,命中就跳对应D#、高亮顶部tab
- **不在范围内**(提前打开/行程结束后还留着):显示"倒计时X天开始"或"行程已结束"的提示页,不强行落到D1或D9(选定方案C)

**倒计时**(app.js):
- 遍历当天`items`按时间排序,自动判断"现在正卡在哪一项或下一项还没开始",倒计时到下一项开始时间
- 已经过去的项目自动加灰色样式(纯按时间计算,和用户手动打勾的清单是两套独立机制,互不影响)(选定方案A)

**地图缓存**(map.js):
- 首页放一个"离线缓存全部地图"按钮,出发前(或到日本后连酒店wifi)点一次,把9天路线周边的瓦片一次性存进Service Worker的Cache Storage
- 缓存精度定在中等(3–4级缩放,够看清路线和地标,不追求放大到街道门牌级别),预计总占用**控制在50MB以内**(选定方案A,一次性全部缓存优于按天缓存或完全不做离线地图)

**清单**(checklist.js):
- 读`checklist.json`渲染列表,每条可点击标记"已处理",状态存localStorage的`{id: true/false}`对象
- 每天行程页里的行程项本身也显示✅/⚠状态,但那是只读的(状态由数据文件决定,不是用户手动点的),和清单页的可勾选列表是两回事(选定方案A:两者都要)

## 离线兜底

- 地图没预热过又没信号:该区域显示"需要联网,或提前在有wifi时点『离线缓存地图』"的占位提示,不空白卡死
- 行程数据(`d#.json`)体积很小,会跟着App壳一起被Service Worker缓存,基本不会出现"没数据"的情况

## 部署

- 新建独立文件夹`D:\Projects\日本旅游\pwa\`(现有行程文档所在目录不是git仓库,且不适合把代码和旅行文档混在一起)
- 本地`git init`,GitHub远程仓库用**已经登录的GitHub CLI账号(kkcarry123)**创建,不需要用户重新输入密码/登录
- 部署到GitHub Pages(免费,PWA在iOS上安装到主屏需要的https条件也满足)

## 卸载/清理

App以"添加到主屏幕"方式安装。旅行结束后:长按主屏幕图标 → 删除App → 确认。iOS(16.4+)会把主屏幕网页应用的存储(Service Worker缓存的地图瓦片、localStorage的打勾状态)当作独立空间,删除图标时一并清空,不会有残留垃圾。这一点会在测试阶段实机验证。

## 测试计划

写完代码后在用户iPhone上实机过一遍:
1. 添加到主屏幕,确认是全屏无地址栏的独立体验
2. 开飞行模式测离线:地图(需已预热)、清单、倒计时都要能正常用
3. 点几下清单,完全关闭App重新打开,确认勾选状态还在
4. 临时改测试用的日期,验证"不在行程范围内"时的提示页
5. 长按删除App,确认占用空间被清空、无残留

## 后续行程数据更新流程

行程本身(D:\Projects\日本旅游\行程确定版-0912-0920.md)每次有新决定(比如某家餐厅确认预约成功),Claude会同步修改对应的`data/d#.json`(以及`checklist.json`里对应条目的去留),行程文档和App数据保持同步,用户不需要手动维护两份。

## 部署地址

https://kkcarry123.github.io/japan-trip-pwa/
